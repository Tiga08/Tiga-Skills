@AGENTS.md

## 约束

- 保持本仓库为「内容加 Bash」的形态。除非用户提出要求，不要引入应用框架、构建系统或运行时依赖。
- 脚本保持为 UTF-8 编码的可执行 Bash，沿用既有的直接命令风格；修改任何 shell 脚本后都要运行 `bash -n`。

## 常见陷阱

1. **混淆三处技能目录。** `.agents/skills/` 放操作本仓库的项目级技能，`03-custom-skills/` 放自定义注册表源文件，`02-agent-skills/` 只放注册用的符号链接。先把注册链接解析到目标，再判断改哪份源文件才安全。

2. **把 `.tiga/` 当作项目内容。** 该目录被 git 忽略，从来不是权威内容；个人计划放在 `.tiga/Todo.md`。只有用户明确要求时，才把其中内容提升到正式目录。

3. **沿用旧的目录布局。** 当前布局是 `01-prompts/`、`02-agent-skills/`、`03-custom-skills/`、`04-scripts/`。不要重新引入已移除的目录，例如 `00-skill-index/`、`03-workflows/`、`05-custom-skills/`。
